<table class="ui celled table definition tubes">
  <tbody>
  {{#each tubes as |tube index|}}
    {{#if (or displayAllTubes tube.hasProductionChallenge) }}
      <tr>
        <td>
          {{#if (eq view "skills")}}
            {{#link-to "competence.tube.index" competenceModel tube}}{{tube.name}}{{/link-to}}
          {{else}}
            {{tube.name}}
          {{/if}}
        </td>
        {{#each tube.filledSkills as |skill|}}
         {{competence/grid/grid-cell view=view skill=skill link=link tubeId=tube.id competenceModel=competenceModel index=index}}
        {{/each}}
      </tr>
    {{/if}}
  {{/each}}
  </tbody>
</table>
