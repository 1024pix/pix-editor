<div class="ui grid">
  <h2 id="competence-profile-anchor-{{competence.id}}" class="row competence-profile {{concat "area-profile-" areaCode}}">
    <div class="column center competence-code">
      <p>{{competence.code}}</p>
    </div>
    <div class="fourteen wide column">
      <div class="competence-titre">{{competence.title}}</div>
      <div class="competence-description">{{competence.description}}</div>
    </div>
  </h2>
  <div class="row tubes-profile">
    <div class="four wide column tube-description">
      <p></p>
    </div>
    <div class="twelve wide column">
      {{#each filterTube as |tube|}}
        <div class="tube-profile" {{action "showTube" tube}}>

          <div class={{if tube.selectedLevel "active"}}>
            <i class="square icon {{if tube.selectedLevel "active check" "outline"}}">
            </i>
            {{#if showTubeDetails}}
              <div class="max-skill-level"><p>{{if tube.selectedLevel tube.selectedLevel ""}}</p></div>
            {{/if}}
            <div class="practicalTitle-profile {{if tube.selectedLevel " active"}}">
              <p>{{if showTubeDetails tube.name tube.practicalTitle }}</p></div>
          </div>
          <p class="practicalDescription-profile {{concat
                  (if tube.selectedLevel " active" "")}}">{{tube.practicalDescription}}</p>
        </div>
      {{/each}}
    </div>
  </div>
  <p class="selected-tube-count">{{competence.selectedProductionTubeCount}}/{{competence.productionTubeCount}}</p>
</div>
