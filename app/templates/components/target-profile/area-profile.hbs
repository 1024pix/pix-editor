{{#each this.filteredCompetences as |competence|}}
  <TargetProfile::CompetenceProfile @competence={{competence}} @areaCode={{@area.code}} @displayTube={{@displayTube}} @level={{@level}} @selectedSkills={{@selectedSkills}} @showTubeDetails={{@showTubeDetails}} @clearTube={{@clearTube}} @setTubeLevel={{@setTubeLevel}} @filter={{@filter}} />
{{/each}}