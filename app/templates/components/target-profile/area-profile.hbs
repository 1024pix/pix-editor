{{#each this.filterCompetences as |competence|}}
  <TargetProfile::CompetenceProfile @competence={{competence}} @areaCode={{this.area.code}} @displayTube={{this.displayTube}} @level={{this.level}} @selectedSkills={{this.selectedTubeSkills}} @showTubeDetails={{this.showTubeDetails}} @clearTube={{this.clearTube}} @setTubeLevel={{this.setTubeLevel}} @filter={{this.filter}} />
{{/each}}