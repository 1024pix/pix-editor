<div class="field {{if edition "" " disabled"}}">
    <label>
        {{title}}
    </label>
    {{#if edition}}
        {{#ui-search class=(concat "search-tuto-input") showNoResults=true maxResults=5 apiSettings=searchAPISettings
                     error=searchErrors onSelect=(action "selectTutorial" tutorials)}}
            <div class="ui icon input">
                <input class="prompt" placeholder="Ajouter/créer tutoriel" type="text">
                <i class="search icon"></i>
            </div>
            <div class="results"></div>
        {{/ui-search}}
    {{/if}}
</div>
<div >
    {{#if loading}}
        <div class="ui active centered inline loader"></div>
    {{else}}
        <div class="ui cards">
            {{#each tutorials as |tutorial|}}
                <div class="card">
                    <div class="content">
                        <div class="header">
                            {{#if edition}}
                                <div {{action "unselectTutorial" tutorials tutorial}} class="ui right floated icon button">
                                    <i class="close icon"></i>
                                </div>
                            {{/if}}
                            <a class="ui right floated button basic" href={{tutorial.link}} target="_blank"
                               rel="noopener">
                                <i class="external icon"></i>
                            </a>
                            {{tutorial.title}}
                        </div>
                        <div class="description">
                            <div>Format : {{tutorial.format}}</div>
                            <div>Durée : {{tutorial.duration}}</div>
                            <div>Source : {{tutorial.source}}</div>
                        </div>
                    </div>
                </div>
            {{/each}}
            {{#if empty}}
                <div class="card">
                    <div class="content">
                        aucun élément
                    </div>
                </div>
            {{/if}}

        </div>
    {{/if}}
</div>
{{create-tutorial item=titleSearch tutorials=tutorials application=application class=popinCreateTutoClass titleSearch=titleSearch }}
