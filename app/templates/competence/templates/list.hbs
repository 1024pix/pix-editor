<div class="main-title  {{if this.config.lite "lite" ""}}">
  <h1 class="ui header">
  <div class="ui right floated menu">
    <button class="ui button icon item" {{on "click" this.close}} type="button"><i class="icon window close"></i></button>
  </div>
      Prototypes de {{this.model.name}}
  </h1>
</div>
<div class="ui attached segment competence {{this.size}} {{this.listHidden}}">
  <List::Templates @list={{this.model.sortedTemplates}} @itemCount={{this.model.templates.length}} @competenceModel={{this.competence}} @hidden={{false}} />
</div>
<div class="ui borderless bottom attached labelled icon menu {{this.listHidden}}">
  {{#if this.mayCreateTemplate}}
    <button class="ui button right item" {{on "click" this.newVersion}} type="button">
      <i class="plus square outline icon"></i> Nouvelle version
    </button>
  {{/if}}
</div>
{{outlet}}
