<div class="main-title  {{if config.lite "lite" ""}}">
  <h1 class="ui header">
  <div class="ui right floated menu">
    <a class="ui icon item" onclick={{action "close"}}><i class="icon window close"></i></a>
  </div>
      Prototypes de {{model.name}}
  </h1>
</div>
<div class="ui attached segment competence {{size}} {{listHidden}}">
  {{templates-list list=model.sortedTemplates itemCount=model.templates.length competenceModel=competence hidden=false}}
</div>
<div class="ui borderless bottom attached labelled icon menu {{listHidden}}">
  {{#if mayCreateTemplate}}
    <a class="right item" onclick={{action "newVersion"}}>
      <i class="plus square outline icon"></i> Nouvelle version
    </a>
  {{/if}}
</div>
{{outlet}}
