Ce répertoire contient une version patchée de `css-element-queries` nécessaire pour qu'`ember-table` soit fonctionnel avec Vite.
Ce répertoire pourra être supprimé quand `ember-table` ne sera plus utilisé.
