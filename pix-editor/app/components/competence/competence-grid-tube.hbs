<td data-test-tube-cell class="tube-cell">
  {{#if (eq @section "skills")}}
    <LinkTo data-test-tube-managment @route="authenticated.competence.tubes.single" @model={{@tube}}>{{this.name}}</LinkTo>
  {{else}}
    {{this.name}}
  {{/if}}
</td>

{{#each this.skillOverviewsOrSkills as |skillOverviewOrSkill index|}}
  <Competence::Grid::GridCell
    @section={{@section}}
    @languageFilter={{@languageFilter}}
    @view={{@view}}
    @skill={{skillOverviewOrSkill.skill}}
    @skills={{skillOverviewOrSkill.skills}}
    @skillOverview={{skillOverviewOrSkill.skillOverview}}
    @link={{@link}}
    @tube={{@tube}}
    @tubeId={{@tubeOverview.airtableId}}
    @index={{index}}
  />
{{/each}}
