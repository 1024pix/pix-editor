<div class={{concat "field" (if @edition "" " disabled")}} ...attributes>
  <label>{{@title}}</label>
  <div class="ui input">
    {{#if @label}}
      <div class="label-input">{{@label}} : </div>
    {{/if}}
    <Input @value={{@value}} @readonly={{not @edition}} />
  </div>
</div>
