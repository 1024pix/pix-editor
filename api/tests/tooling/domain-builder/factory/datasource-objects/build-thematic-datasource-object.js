export function buildThematicDatasourceObject(
  {
    id = 'recFvllz2Ckz',
    competenceId = 'recCompetence0',
    tubeIds = ['recTube0'],
    index = 0
  } = {}) {
  return {
    id,
    competenceId,
    tubeIds,
    index,
  };
}
