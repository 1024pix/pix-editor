export class EmbedConfig {
  constructor({
    id,
    embedId,
    name,
    data,
    sha,
  }) {
    this.id = id;
    this.embedId = embedId;
    this.name = name;
    this.data = data;
    this.sha = sha;
  }
}
