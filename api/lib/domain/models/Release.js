module.exports = class Release {
  constructor({
    id,
    // attributes
    content,
    createdAt,
    // includes
    // references
  } = {}) {
    this.id = id;
    // attributes
    this.content = content;
    this.createdAt = createdAt;
    // includes
    // references
  }
};
