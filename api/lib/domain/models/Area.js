class Area {

  constructor({
    id,
    // attributes
    code,
    name,
    // includes
    // references
    competenceIds,
  } = {}) {
    this.id = id;
    // attributes
    this.code = code;
    this.name = name;
    // includes
    // references
    this.competenceIds = competenceIds;
  }
}

module.exports = Area;
